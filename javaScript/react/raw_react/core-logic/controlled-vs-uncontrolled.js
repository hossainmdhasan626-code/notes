/**
 *   Controlled vs Uncontrolled Components (ডিপ ডাইভ নোট)
 * ---------------------------------------------------------
 * ফোল্ডার: core-topics
 * ফাইল নাম: controlled-vs-uncontrolled.js
 * * মূল কথা: ডাটা বা 'State' কার নিয়ন্ত্রণে? রিঅ্যাক্ট স্টেট নাকি ব্রাউজারের DOM?
 */

import { useState, useRef } from 'react';

/**
 * ১. Controlled Component (রিঅ্যাক্টের নিয়ন্ত্রণে):
 * --------------------------------------------
 * এখানে ইনপুট ফিল্ডের ভ্যালু 'useState' দিয়ে কন্ট্রোল করা হয়। 
 * ইউজার একটি বাটন চাপলে 'onChange' দিয়ে স্টেট আপডেট হয়, 
 * আর সেই স্টেট আবার ইনপুটের 'value' হিসেবে স্ক্রিনে দেখায়।
 * * হাসানের লজিক: এটি হলো "সরাসরি নাক ধরা"। 
 * ডাটা প্রতিটি মুহূর্তে আমার (ডেভেলপারের) হাতে থাকে।
 */

function ControlledInput() {
  const [email, setEmail] = useState("");
  const [error, setError] = useState("");

  const handleChange = (e) => {
    const val = e.target.value;
    setEmail(val);

    // --- রিয়েল-টাইম ভ্যালিডেশন (সাবমিট করার আগেই) ---
    // যেহেতু ডাটা প্রতি মুহূর্তে আমার হাতে আসছে, তাই আমি চেক করতে পারছি।
    if (!val.includes("@")) {
      setError("সঠিক ইমেইল দরকার! (@ থাকতে হবে)");
    } else {
      setError("");
    }
  };

  return (
    <div style={{ padding: '10px', border: '1px solid blue' }}>
      <h3>Controlled Form (প্রফেশনাল পদ্ধতি)</h3>
      <input 
        value={email} 
        onChange={handleChange} 
        placeholder="ইমেইল লিখুন"
      />
      {error && <p style={{ color: 'red' }}>{error}</p>}
      <p>লাইভ ডাটা: {email}</p>
    </div>
  );
}

/**
 * ২. Uncontrolled Component (DOM-এর নিয়ন্ত্রণে):
 * ----------------------------------------------
 * এখানে রিঅ্যাক্ট স্টেট থাকে না। ইনপুট ফিল্ড নিজেই নিজের ডাটা ধরে রাখে। 
 * ডাটা দরকার হলে 'useRef' দিয়ে সরাসরি DOM থেকে "টেনে" আনতে হয়।
 * * হাসানের লজিক: এটি হলো "মাথার পেছন দিয়ে হাত ঘুরিয়ে নাক ধরা"।
 * ডাটা আমার হাতে নেই, দরকার হলে DOM-এর কাছে গিয়ে "ভিক্ষা" বা "ধার" করতে হয়।
 */

function UncontrolledInput() {
  const inputRef = useRef(null);

  const handleSubmit = () => {
    // শুধুমাত্র সাবমিট করলেই ডাটা পাওয়া যায়
    const val = inputRef.current.value;
    alert("ইউজার লিখেছে: " + val);
  };

  return (
    <div style={{ padding: '10px', border: '1px solid gray', marginTop: '10px' }}>
      <h3>Uncontrolled Form (পুরানো পদ্ধতি)</h3>
      <input type="text" ref={inputRef} placeholder="কিছু লিখুন..." />
      <button onClick={handleSubmit}>সাবমিট</button>
      {/* সমস্যা: ইউজার ভুল লিখলে টাইপ করার সময় থামানোর কোনো উপায় নেই */}
    </div>
  );
}

/**
 * ৩. তুলনা (হাসানের এনসাইক্লোপিডিয়া স্পেশাল):
 * -----------------------------------------
 * | ফিচার             | Controlled Component       | Uncontrolled Component    |
 * |-------------------|----------------------------|---------------------------|
 * | তথ্যের উৎস (Truth) | রিঅ্যাক্ট স্টেট (লাইভ)        | ব্রাউজার DOM (আড়ালে)       |
 * | ভ্যালিডেশন         | রিয়েল-টাইম (প্রতি ক্লিকে)      | সাবমিট করার পর             |
 * | ইনপুট ফিল্টার      | খুব সহজে করা যায়            | প্রায় অসম্ভব               |
 * | কোডের পরিমাণ       | একটু বেশি কোড লাগে           | কম কোডে হয়ে যায়           |
 */

/**
 * ৪. কেন Uncontrolled ভ্যালিডেশনের জন্য "খারাপ"? (The "Why"):
 * ---------------------------------------------------------
 * ১. ইনস্ট্যান্ট ফিডব্যাক নেই: ইউজার ভুল করলে সাথে সাথে ওয়ার্নিং দেওয়া যায় না।
 * ২. ইনপুট মাস্কিং: ইউজার যেন শুধু সংখ্যা টাইপ করে, সেটা টাইপিংয়ের সময় আটকানো যায় না।
 * ৩. নন-সিঙ্ক: অন্য কোনো কম্পোনেন্ট যদি এই ডাটার ওপর নির্ভর করে, তবে তা আপডেট হয় না।
 */

/**
 * ৫. হাসান'স ডিকশনারি (এক কথায়):
 * ----------------------------
 * ১. Controlled: রিঅ্যাক্ট স্টেট-ই সব। ডেভেলপারের ফুল কন্ট্রোল থাকে।
 * ২. Uncontrolled: DOM-ই সব। ডেভেলপার শুধু দরকারের সময় ডাটা ধার করে আনে।
 */