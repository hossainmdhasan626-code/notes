/**
 * ৪৬. ইঞ্জিনের পরিচয়পত্র (Engine Identity)
 * -------------------------------------
 * ফোল্ডার: raw_js/core-topic
 * ফাইল নাম: engine-identity-map.js
 */

// ১. V8 Engine (Standalone Engine): 
// কাজ: JS Logic কম্পাইল করা এবং CPU-তে চালানো। এটি Blink-এর অংশ নয়।

// ২. Blink Engine (Standalone Engine): 
// কাজ: UI রেন্ডার করা। এটি V8-এর অংশ নয়।

// ৩. HTML & CSS Parsers: 
// এরা স্বতন্ত্র ইঞ্জিন নয়, এরা Blink-এর ভেতরে থাকা দুটি 'স্পেশালিস্ট মডিউল'।

/**
 * হাসানের জন্য প্রো-টিপ:
 * V8 এবং Blink-এর মধ্যে যে যোগাযোগ হয়, সেটা হয় 'Binding' বা 'Web IDL' এর মাধ্যমে। 
 * এরা একে অপরের ওপর নির্ভরশীল, কিন্তু এরা আলাদা দুটি সত্তা।
 */