/**
 * ==========================================================
 * TOPIC: JAVASCRIPT CONSTRUCTOR FUNCTIONS (মাস্টার নোট)
 * Author: Hasan (Junior Frontend Developer)
 * ==========================================================
 * * ১. কনস্ট্রাক্টর কী এবং কেন ব্যবহার হয়?
 * ----------------------------------
 * কনস্ট্রাক্টর হলো একটি স্পেশাল ফাংশন যা নতুন অবজেক্ট তৈরি (Initialize) করতে ব্যবহৃত হয়।
 * * কেন ব্যবহার করব?
 * - Reusability: একই ধরণের অনেক অবজেক্ট বারবার তৈরি করার জন্য।
 * - Efficiency: অবজেক্টের প্রোপার্টি এবং মেথডগুলো এক জায়গায় ম্যানেজ করার জন্য।
 * - Consistency: সব অবজেক্ট যেন একই স্ট্রাকচার মেনে চলে তা নিশ্চিত করতে।
 */

// ২. কনস্ট্রাক্টর লেখার কনভেনশন (Naming Convention)
// ---------------------------------------------
// - কনস্ট্রাক্টর ফাংশনের নাম সবসময় Capital Letter (PascalCase) দিয়ে শুরু করতে হয়।
// - উদাহরণ: Person, Product, Order (এটি নিয়ম নয়, কিন্তু ডেভেলপারদের মধ্যে স্ট্যান্ডার্ড)।

function Device(name, brand) {
    this.name = name;
    this.brand = brand;
    this.turnOn = function() {
        console.log(this.name + " is turning on...");
    };
}

const laptop = new Device("MacBook", "Apple");



// ৩. কনস্ট্রাক্টর কি অ্যারো ফাংশন (Arrow Function) দিয়ে লেখা যায়?
// --------------------------------------------------------
// উত্তর: না। অ্যারো ফাংশন দিয়ে কনস্ট্রাক্টর লেখা যায় না।
// কারণ: 
// - অ্যারো ফাংশনের নিজস্ব 'this' নেই।
// - অ্যারো ফাংশনকে 'new' কিউয়োর্ড দিয়ে কল করা যায় না।
// - এদের কোনো 'prototype' প্রোপার্টি থাকে না।

/* const IllegalCon = (name) => { this.name = name; };
const obj = new IllegalCon("Hasan"); // TypeError দিবে!
*/


// ৪. কনস্ট্রাক্টর লেখার বিভিন্ন পদ্ধতি (Ways to write)
// ---------------------------------------------

// পদ্ধতি ক) Function Declaration (পুরানো ও পপুলার পদ্ধতি)
function User(name) {
    this.name = name;
}

// পদ্ধতি খ) ES6 Classes (আধুনিক পদ্ধতি - বর্তমানে এটিই সবথেকে বেশি ব্যবহৃত হয়)
class Member {
    constructor(name, email) {
        this.name = name;
        this.email = email;
    }
    getInfo() {
        return `${this.name} (${this.email})`;
    }
}

const m1 = new Member("Hasan", "hasan@dev.com");




// ৫. জাভাস্ক্রিপ্টের বিল্ট-ইন কনস্ট্রাক্টর (Built-in Constructors)
// -----------------------------------------------------------
// জাভাস্ক্রিপ্টে অনেক আগে থেকেই কিছু কনস্ট্রাক্টর দেওয়া আছে যা আমরা প্রতিদিন ব্যবহার করি:

const obj = new Object();   // {} তৈরির জন্য
const str = new String();   // String অবজেক্টের জন্য
const num = new Number();   // Number অবজেক্টের জন্য
const bool = new Boolean(); // Boolean অবজেক্টের জন্য
const arr = new Array();    // [] তৈরির জন্য
const date = new Date();    // বর্তমান তারিখ ও সময়ের জন্য
const map = new Map();      // Map ডেটা স্ট্রাকচারের জন্য
const set = new Set();      // Set ডেটা স্ট্রাকচারের জন্য


// ৬. ইন্টারভিউ টিপস: 'new' কিউয়োর্ড কী করে?
// -------------------------------------
// ১. একটি নতুন খালি অবজেক্ট {} তৈরি করে।
// ২. 'this' কে ওই অবজেক্টের সাথে লিঙ্ক করে।
// ৩. অবজেক্টের প্রোটোটাইপ সেট করে।
// ৪. ফাংশন শেষে অবজেক্টটি রিটার্ন করে।

/**
 * হাসান, মনে রাখবে: 
 * রিয়্যাক্ট বা আধুনিক জাভাস্ক্রিপ্ট প্রজেক্টে (যেমন: e-comark) 
 * এখন 'class' কিউয়োর্ড বেশি ব্যবহৃত হয়, তবে এর পেছনে কাজ করে এই কনস্ট্রাক্টর ফাংশনই।
 */